const { clientFields, clientMutations } = require("./clients");
const { projectFields, projectMutations } = require("./projects");

module.exports = {
  clientFields,
  projectFields,
  clientMutations,
  projectMutations,
};
